import React from "react";
import ReactDOM from "react-dom/client";
import OrderPage from "./container/OrderPage";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <OrderPage id="ORD-0001" />
  </React.StrictMode>
);
